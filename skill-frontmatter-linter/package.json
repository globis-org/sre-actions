{
  "name": "skill-frontmatter-linter",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "main": "dist/main.js",
  "scripts": {
    "type-check": "tsc",
    "package": "esbuild src/main.ts src/cli.ts --bundle --platform=node --target=node24 --outdir=dist --sourcemap",
    "all": "pnpm type-check && pnpm package"
  },
  "dependencies": {
    "@actions/core": "3.0.1",
    "yaml": "2.9.0"
  },
  "devDependencies": {
    "@types/node": "25.9.5"
  }
}
