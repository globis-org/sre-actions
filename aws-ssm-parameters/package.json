{
  "name": "aws-ssm-parameters",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "main": "dist/main.js",
  "scripts": {
    "type-check": "tsc",
    "build": "esbuild src/main.ts --bundle --platform=node --target=node20 --outdir=dist --sourcemap",
    "all": "yarn type-check && yarn build"
  },
  "dependencies": {
    "@actions/core": "1.10.0",
    "@aws-sdk/client-ssm": "3.205.0",
    "js-yaml": "4.1.0",
    "zod": "3.19.1"
  }
}
